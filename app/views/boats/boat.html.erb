<div class="deconstructed">
  Mes Bateaux
  <div>Mes Bateaux</div>
  <div>Mes Bateaux</div>
  <div>Mes Bateaux</div>
  <div>Mes Bateaux</div>
</div>

<div class="card-product">
  <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" />
  <div class="card-product-infos">
    <h2>Product name</h2>
    <p>Product description with <strong>relevant info</strong> only.</p>
  </div>
  <%= link_to(<i class="fas fa-edit"></i>, edit_boat_path(@boat), class: 'btn btn-primary') %>
    <%= link_to("Delete my boat", boat_path(@boat),
          method: :delete,
          data: { confirm: "Are you sure?" }) %>
</div>

<br>
<h2>Name:<%= @boat.name %></h2>
<h2>Owner:<%= @boat.owner.first_name %></h2>
<%= link_to("Edit my boat", edit_boat_path(@boat)) %>
<%= link_to("Delete my boat", boat_path(@boat),
      method: :delete,
      data: { confirm: "Are you sure?" }) %>
<div class="d-flex justify-content-center location-button">
<p class="btn-log"><%= link_to("Back to my boats", my_boats_path) %></p>
</div>

