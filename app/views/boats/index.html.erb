<div class="custom-container">
  <h1 class="">Bateaux à louer près de <%= @city %></h1>
  <br>
  <div id="search-results-container">
    <%= render partial: 'boat_cards', locals: {boats: @boats} %>
    <div
      id="map"
      style="width: 100%;
      height: 70vh;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
    ></div>
  </div>
</div>