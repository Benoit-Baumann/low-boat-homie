 <h2>Bateaux à louer près de <%= @city %></h2>
    <div id="container">
    <%= render partial: 'boat_cards', locals: {boats: @boats} %>
      <div
        id="map"
        style="width: 100%;
        height: 70vh;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
      ></div>
    </div>
