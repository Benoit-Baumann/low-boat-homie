   <div class="card-boat">
 <% if boat.photo? %>
        <% background_url = cl_image_path boat.photo, width: 400, height: 500, crop: :fill %>
    <% else %>
        <% background_url = image_path '75604.jpg' %>
    <% end %>
    <img src="<%= background_url %>" />
  <div class="content-window">
    <h4><%= boat.name %></h4>
    <p><%= boat.price %> €/jour</p>
  </div>
</div>
